let user={
    userName: "Mohamed",
    userAge: 24,
    userGender: "Male"
};
// console.log(user.userName.length);
    for (let i =0; i < user.userName.length; i++) {
    console.log(user.userName[i]);
}